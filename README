Dependances:
    python 3.3
    matplotlib >=1.2
    NumPy >=1.6
    Scipy >=0.11
    PyQt >=4.9
    IPython >=1.0
    pygments
    pyzmq


Install:

    set CFLAFS (optional):
        export CFLAGS='-O3 -mtune=native -march=native'

    install system:
        python setup.py install
    
    install inplace:
        python setup.py build --buid-lib .
